# Skills by Workflow Domain

Which skills to recommend based on the user's workflow.

## Code Development

| Skill | Purpose |
|-------|---------|
| `code-review` | PR and code quality review |
| `frontend-design` | UI component creation |
| `shadcn-ui` | shadcn/ui + Tailwind components |
| `parallel-worktree-setup` | Multi-branch parallel dev |

**Policy implications:** Needs shell access (git, npm/node, test runners). Needs project directory write access.

## Content Creation (MyMoneyCoach.ai)

| Skill | Purpose |
|-------|---------|
| `storybrand-content-engine` | Weekly blog posts (StoryBrand) |
| `circular-soundbite-generator` | Social media from blog content |
| `sophia-image-generator` | Sophia character images |
| `content-calendar-orchestrator` | Content planning and tracking |
| `creative-stretch` | Make content bolder |
| `peace-soundbites` | PEACE framework messaging |
| `mymoneycoach-audience-profile` | Audience targeting data |

**Policy implications:** Needs image output directory write access. Needs MCP: nanobanana, asset-intelligence. May need Google Workspace MCP for scheduling.

## Document Generation

| Skill | Purpose |
|-------|---------|
| `pdf` | PDF extraction and creation |
| `docx` | Word document creation |
| `pptx` | PowerPoint generation |
| `xlsx` | Excel spreadsheet manipulation |
| `linkedin-carousel` | LinkedIn carousel PPTX |

**Policy implications:** Needs write access to output directory. Shell: python3 if using scripts.

## Image Generation

| Skill | Purpose |
|-------|---------|
| `image-generation-workflow` | Master image orchestrator |
| `sophia-image-generator` | Sophia character (Pixar 3D) |
| `rich-image-generator` | Rich character images |
| `jennifer-image-generator` | Jennifer character images |
| `ai-image-model-selector` | Choose best model for task |
| `meta-ad-creative` | Meta ad images |

**Policy implications:** Needs MCP: nanobanana-remote, asset-intelligence. Needs write access to image output directory.

## DevOps / Infrastructure

| Skill | Purpose |
|-------|---------|
| `mcp-builder` | Build MCP servers |
| `mac-mini-ops` | Remote Mac Mini control |

**Policy implications:** Broader shell access needed. May need network access to infrastructure endpoints.

## Quality Assurance

| Skill | Purpose |
|-------|---------|
| `qa-tester` | Browser automation testing |
| `code-review` | Code quality checks |

**Policy implications:** Needs MCP: chrome-devtools. Shell: node, npm for test runners.

## SEO / Marketing

| Skill | Purpose |
|-------|---------|
| `seo-page-audit` | Page SEO analysis |
| `h1-title-alignment` | Title/H1 alignment audit |
| `synthetic-seo-testing` | Synthetic user testing |

**Policy implications:** May need network access for fetching pages. Needs chrome-devtools MCP for live page analysis.

## Job Applications

| Skill | Purpose |
|-------|---------|
| `storybrand-ats-resume-builder` | Resume + cover letter |
| `richard-yaker-job-applications` | Rich's application materials |

**Policy implications:** Minimal — mainly document generation. Needs write access to output directory.
