{
  "name": "zora",
  "version": "0.5.0",
  "description": "Long-running autonomous personal AI agent for macOS — multi-provider, capability-routed, memory-enriched",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "zora": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT",
  "author": "Rich Yaker <ryaker@yaker.org>",
  "repository": {
    "type": "git",
    "url": "https://github.com/ryaker/zora.git"
  },
  "keywords": [
    "ai-agent",
    "autonomous",
    "claude",
    "gemini",
    "macos",
    "llm",
    "orchestrator"
  ],
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.39",
    "smol-toml": "^1.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
